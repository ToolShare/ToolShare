extends layout

block content
  .container-fluid
    .row
      h2 Available #{tools[0].name}
      div.list-group
        each tool in tools
          a(class="list-group-item" href="/toolRequest/#{tool.id}")= tool.userId
